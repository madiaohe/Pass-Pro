# PassPro - 桌面密码管理工具

一款基于 Electron + React + Tailwind CSS 开发的现代化桌面密码管理应用。

## ✨ 特性

- 🔐 **AES-256-GCM 加密** - 本地加密存储，数据安全
- 🎨 **现代化 UI** - 玻璃拟态设计，流畅动效
- 🗂️ **分类管理** - 社交、金融、工作、娱乐分类
- 🔍 **快速搜索** - 实时搜索密码条目
- 📋 **一键复制** - 快速复制用户名和密码
- 🎲 **密码生成器** - 自动生成强密码
- 💾 **本地存储** - 数据完全本地，无需联网

## 🚀 快速开始

### 环境要求

- Node.js >= 18
- npm 或 yarn

### 安装步骤

1. **安装依赖**

```bash
npm install
```

2. **开发模式运行**

```bash
npm run dev
```

3. **构建应用**

```bash
# Windows
npm run electron:build:win

# macOS
npm run electron:build:mac

# Linux
npm run electron:build:linux
```

## 📁 项目结构

```
passpro/
├── electron/               # Electron 主进程
│   ├── main.js            # 主进程入口
│   ├── preload.js         # 预加载脚本
│   └── utils/
│       └── crypto.js      # 加密模块
├── src/                    # React 前端
│   ├── App.jsx            # 主应用组件
│   ├── main.jsx           # React 入口
│   ├── components/        # UI 组件
│   ├── hooks/             # 自定义 Hooks
│   └── styles/            # 全局样式
├── dist/                  # 构建输出
├── package.json
├── vite.config.js
├── tailwind.config.js
└── index.html
```

## 🔒 安全说明

### 加密机制

1. **密钥派生**: 使用 PBKDF2 算法，100,000 次迭代
2. **加密算法**: AES-256-GCM
3. **数据存储**: 加密后存储在用户目录 `~/.passpro/passwords.enc`
4. **主密码**: 使用 bcrypt 哈希存储，不保存明文

### 安全建议

- 设置强主密码（至少 8 位，包含大小写和特殊字符）
- 定期备份 `~/.passpro/passwords.enc` 文件
- 不要将主密码保存在其他地方

## 🛠️ 技术栈

| 技术 | 用途 |
|-----|------|
| Electron | 桌面应用框架 |
| React 18 | UI 框架 |
| Vite | 构建工具 |
| Tailwind CSS | 样式 |
| Lucide React | 图标 |
| Node.js Crypto | 加密 |

## 📜 脚本说明

```bash
npm run dev              # 启动 Vite 开发服务器
npm run build            # 构建前端代码
npm run electron:dev     # 开发模式运行 Electron
npm run electron:build   # 构建所有平台
npm run electron:build:win   # 构建 Windows 版本
npm run electron:build:mac   # 构建 macOS 版本
npm run electron:build:linux # 构建 Linux 版本
```

## 🐛 常见问题

### Q: 数据存储在哪里？

A: 数据存储在用户目录下的 `.passpro` 文件夹：
- Windows: `C:\Users\<用户名>\.passpro\`
- macOS: `~/.passpro/`
- Linux: `~/.passpro/`

### Q: 忘记主密码怎么办？

A: 由于使用端到端加密，忘记主密码将无法恢复数据。请务必牢记主密码。

### Q: 如何备份数据？

A: 复制 `~/.passpro/passwords.enc` 文件到安全位置即可。

### Q: 可以在多台电脑上使用吗？

A: 可以，只需复制 `~/.passpro/` 文件夹到新电脑即可。

## 📝 开发计划

- [ ] 自动锁定功能
- [ ] 密码强度检测
- [ ] 导入/导出功能
- [ ] 生物识别解锁
- [ ] 主题切换

## 📄 许可证

MIT License

---

Made with ❤️ by PassPro Team
